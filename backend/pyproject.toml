[project]
name = "chen-assistant"
version = "0.1.0"
description = "here is my dream."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.1",
    "langchain==1.2",
    "uvicorn>=0.46.0",
]
